using System;
using System.Collections.Generic;
using System.Text;
using System.Numerics;

namespace SplinterCamoForAlex
{
    class RandomVertexSet
    {
        //Create a class that can generate a VertexPositionColour with random coordinate that will be displayed

        // eventually, create a function that generates a new VertexPositionColour that is "local" to the previous one, in that it is near
        // to it.

        // eventually eventually, Createa function that generates a list of VertexPositionColours that are roughly evenly distributed across
        // the screen and are local to eachother, so that there are no large jumps across the screen
    }

}
